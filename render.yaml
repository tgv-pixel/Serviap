services:
  - type: worker
    name: telegram-bot
    env: python
    buildCommand: pip install python-telegram-bot==20.7
    startCommand: python app.py
    envVars:
      - key: BOT_TOKEN
        sync: false  # This means you set it manually in dashboard
